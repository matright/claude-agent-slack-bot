# Environment Variables Configuration for Claude Agent Slack Bot
#
# IMPORTANT: DO NOT commit your actual .env file with real credentials!
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# ============================================================================
# HOW THIS FILE IS USED
# ============================================================================
#
# FOR LOCAL PYTHON DEVELOPMENT:
#   The app uses python-dotenv to automatically load .env file
#   Just run: python -m app
#
# FOR DOCKER COMPOSE DEVELOPMENT:
#   Docker Compose automatically loads .env file from project root
#   Variables are passed to container via environment: section in docker-compose.yml
#   Just run: docker-compose up
#
# FOR PRODUCTION DEPLOYMENT:
#   DO NOT use .env files! Use your platform's secret management:
#   - Kubernetes: Use Secrets and ConfigMaps
#   - Docker Swarm: Use Docker secrets
#   - Cloud platforms: Use AWS Secrets Manager, GCP Secret Manager, etc.
#   - Pass directly: docker run -e SLACK_BOT_TOKEN=xxx -e SLACK_APP_TOKEN=xxx ...
#
# ============================================================================

# ============================================================================
# SLACK CONFIGURATION
# ============================================================================

# Slack Bot User OAuth Token
# Required scope: app_mentions:read, chat:write
# Get from: https://api.slack.com/apps -> Your App -> OAuth & Permissions
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Slack App-Level Token
# Required scope: connections:write (for Socket Mode)
# Get from: https://api.slack.com/apps -> Your App -> Basic Information -> App-Level Tokens
SLACK_APP_TOKEN=xapp-your-app-token-here

# ============================================================================
# ANTHROPIC CONFIGURATION
# ============================================================================

# Anthropic API Key for Claude Agent SDK
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# Anthropic Base URL (optional - for custom endpoints)
# Default: https://api.anthropic.com
# Set this if using a custom endpoint, proxy, or enterprise deployment
# For standard usage, you can leave this as the default or omit it
ANTHROPIC_BASE_URL=https://api.anthropic.com

# ============================================================================
# CLAUDE AGENT SDK CONFIGURATION
# ============================================================================

# Working directory for Claude Agent SDK operations
# This is where Claude will perform file operations (if tools are enabled)
# For Docker: /app/claude-cwd (will be mounted as a volume)
# For local development: ./claude-cwd or any absolute path
CLAUDE_WORKING_DIR=/app/claude-cwd

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# Uncomment to override the default (DEBUG)
# LOG_LEVEL=DEBUG

# Claude model to use (haiku, sonnet, opus)
# Currently hardcoded in app.py, but you can modify the code to use this
# CLAUDE_MODEL=haiku
